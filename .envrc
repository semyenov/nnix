use flake

# Optional: Set custom environment variables for NixOS development
export NIXOS_CONFIG_DIR="$PWD"

# Optional: Add helpful aliases available only in this directory
alias rebuild="sudo nixos-rebuild switch --flake .#nixos"
alias test-build="sudo nixos-rebuild build --flake .#nixos"
alias check="nix flake check"
alias update="nix flake update"
alias show="nix flake show"
alias fmt="nix fmt"

# Show available commands when entering directory
echo "🚀 NixOS Development Environment Loaded"
echo ""
echo "Available commands:"
echo "  rebuild     - Rebuild and switch system configuration"
echo "  test-build  - Build without switching"
echo "  check       - Check flake for errors"
echo "  update      - Update all flake inputs"
echo "  show        - Show flake outputs"
echo "  fmt         - Format Nix files"
echo ""
echo "Nix tools available:"
echo "  nixpkgs-fmt, alejandra, statix, deadnix, nil"
echo ""